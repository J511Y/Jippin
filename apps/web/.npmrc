engine-strict=true
auto-install-peers=true
# Windows 워크트리의 MAX_PATH(260자) 초과로 Turbopack "Module not found" 가 나던 문제 —
# .pnpm 가상 스토어 디렉터리명을 60자로 줄인다(리눅스 CI/Vercel 에는 무해).
virtual-store-dir-max-length=60
