{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jippin.local/contracts/estimate-result.schema.json",
  "title": "EstimateResult",
  "description": "견적 산출 결과. REPORT.estimate 가 RuleEvalResult + 정책 단가표(pricing_policy_ref) 로부터 산출한다. SDD §4.9·§6.3 정본. 특정 단가는 정책에 따라 변경 가능, policy_version 으로 산정 시점 추적.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "total_range",
    "assumptions",
    "policy_version",
    "consultation_required"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "const": "1.1.0",
      "description": "스키마 버전 (semver). 1.1.0: 화면 표시용 items/vat_included/source_url/disclaimer 추가(추가형, 하위호환)."
    },
    "permit_agency_fee_estimate": {
      "$ref": "#/$defs/MoneyRange",
      "description": "행위허가 대행 비용 범위. 운영 정책 v1 예시 단가 = 33만원."
    },
    "fire_panel_estimate": {
      "$ref": "#/$defs/MoneyRange",
      "description": "방화판 설치 비용 범위. 운영 정책 v1 예시 = 가로 길이 × 2만원/m."
    },
    "fire_glass_estimate": {
      "$ref": "#/$defs/MoneyRange",
      "description": "방화유리 설치 비용 범위. 운영 정책 v1 예시 = 유리 자재비 + 시공비 5만원."
    },
    "total_range": {
      "$ref": "#/$defs/MoneyRange",
      "description": "전체 견적 합산 범위. 항목 미산정 시 최저/최고 폭이 넓어질 수 있다."
    },
    "assumptions": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "산정 전제 (예: '대피공간 가로 길이 = 2.4m 가정')."
    },
    "policy_version": {
      "type": "string",
      "minLength": 1,
      "description": "정책 단가표 버전 (예: 'pricing-2026-Q2-v1'). 항상 산정 결과와 함께 기록한다 (SDD §6.3)."
    },
    "variance_notes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "변동 사유 (예: '단가표 누락 항목 1건 → 상담 권고')."
    },
    "consultation_required": {
      "type": "boolean",
      "description": "상담 권장 여부. PRICING_POLICY_MISSING 또는 ESTIMATE_OUT_OF_RANGE 발생 시 true (SDD §4.9 오류·예외)."
    },
    "items": {
      "type": "array",
      "items": { "$ref": "#/$defs/EstimateItem" },
      "description": "화면 표시용 항목 상세 (1.1.0 추가). total_range 산정의 근거 항목을 사람이 읽는 행으로 나열한다 — 미산정 항목(현장 견적)도 포함."
    },
    "vat_included": {
      "type": "boolean",
      "description": "부가세 포함 여부 (1.1.0 추가)."
    },
    "source_url": {
      "type": "string",
      "description": "단가표 정본 링크 (1.1.0 추가, 예: '/faq?category=cost')."
    },
    "disclaimer": {
      "type": "string",
      "description": "변동 가능 안내 문구 (1.1.0 추가). 모든 견적에 첨부한다."
    }
  },
  "$defs": {
    "EstimateItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "label"],
      "description": "견적 항목 1건 (1.1.0 추가). amount 필드가 모두 null 이면 현장/별도 견적 안내 항목.",
      "properties": {
        "code": { "type": "string", "minLength": 1 },
        "label": { "type": "string", "minLength": 1 },
        "amount_min": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "합산되는 고정 최소액(원)."
        },
        "amount_max": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "전제(assumptions) 기반 상한(원). 없으면 상한 미산정."
        },
        "unit_amount": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "단가(원/unit). 치수 미확정 항목의 표기용."
        },
        "unit": {
          "type": ["string", "null"],
          "description": "단가 단위 (예: '원/m')."
        },
        "note": {
          "type": ["string", "null"],
          "description": "항목 부가 설명 (생활어)."
        }
      }
    },
    "MoneyRange": {
      "type": "object",
      "additionalProperties": false,
      "required": ["currency", "min", "max"],
      "properties": {
        "currency": {
          "type": "string",
          "enum": ["KRW"],
          "description": "통화. MVP 는 KRW 만 지원."
        },
        "min": {
          "type": "integer",
          "minimum": 0,
          "description": "최저 예상 금액 (원, 정수)."
        },
        "max": {
          "type": "integer",
          "minimum": 0,
          "description": "최고 예상 금액 (원, 정수). min <= max 는 RULE/REPORT 의 산정 책임."
        },
        "basis": {
          "type": ["string", "null"],
          "description": "산정 기준 한 줄 요약 (예: '가로 길이 2.4m × 20,000원/m')."
        }
      }
    }
  }
}
