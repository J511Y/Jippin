# THIS FILE IS AUTO-GENERATED — DO NOT EDIT BY HAND.
# Source: packages/contracts/schemas/*.schema.json
# Regenerate: pnpm -C packages/contracts run generate
# generated by datamodel-codegen:
#   filename:  rule-eval-result.schema.json

from __future__ import annotations

from datetime import datetime
from enum import Enum
from typing import Literal

from pydantic import AnyUrl, BaseModel, ConfigDict, Field


class Verdict(Enum):
    """
    최종 판정. 집계 우선순위 DENY > WARN > ALLOW > HOLD 는 RULE 모듈 내부에서 강제 (SDD §4.8 데이터 계약).
    """

    ALLOW = "ALLOW"
    WARN = "WARN"
    DENY = "DENY"
    HOLD = "HOLD"


class Code(Enum):
    """
    방화시설/안전시설 코드.
    """

    FIRE_PANEL = "FIRE_PANEL"
    FIRE_GLASS = "FIRE_GLASS"
    EVACUATION_SPACE = "EVACUATION_SPACE"
    SPRINKLER = "SPRINKLER"
    AUTOMATIC_DOOR_CLOSER = "AUTOMATIC_DOOR_CLOSER"


class RequiredFacility(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    code: Code
    """
    방화시설/안전시설 코드.
    """
    label: str = Field(..., min_length=1)
    """
    사용자 노출 라벨.
    """
    measurement_basis: str | None = None
    """
    산정 기준 (예: '대피공간 인접 벽 가로 길이').
    """


class LegalBasis(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    statute: str = Field(..., min_length=1)
    """
    법령명 (예: '건축법 시행령').
    """
    article: str = Field(..., min_length=1)
    """
    조·항·호 (예: '제46조 제5항').
    """
    summary: str = Field(..., min_length=1)
    """
    한 줄 요약.
    """
    url: AnyUrl | None = None
    """
    원문 링크 (있는 경우).
    """


class RuleEvalResult(BaseModel):
    """
    RULE 모듈의 평가 결과. CommonJudgmentSchema 를 입력으로 산출되며, REPORT 와 REPORT.estimate 의 입력이다. SDD §4.8·§5.1 (I-REPORT-EST-01) 정본.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Literal["1.0.0"] = Field(..., pattern="^\\d+\\.\\d+\\.\\d+$")
    """
    스키마 버전 (semver). 본 ADR-0001 / CMP-527 시점 고정값.
    """
    verdict: Verdict
    """
    최종 판정. 집계 우선순위 DENY > WARN > ALLOW > HOLD 는 RULE 모듈 내부에서 강제 (SDD §4.8 데이터 계약).
    """
    required_facilities: list[RequiredFacility]
    """
    필요 방화시설 목록. REPORT.estimate 의 산정 입력.
    """
    permit_required: bool
    """
    행위허가 필요 여부.
    """
    legal_basis: list[LegalBasis] = Field(..., min_length=0)
    """
    법령 근거 목록. 모든 화면에 노출되어야 한다 (FR-REPORT-009).
    """
    ruleset_version: str = Field(..., min_length=1)
    """
    룰셋 카탈로그 버전 (예: '2026-05-01'). 결정성 100%(NFR-QUAL-002) 의 추적 키.
    """
    evaluated_at: datetime
    """
    RULE 평가 시점 (ISO-8601).
    """
    user_message: str | None = None
    """
    사용자에게 표시할 한 줄 요약. RULE 의 메시지 빌더 결과.
    """
